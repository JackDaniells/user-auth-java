var _ = require('underscore');
var app = require('./app.js');

var auth = require('./auth.js');

app.init();

var text = 'teste'
var text2 = 'teste'
//const [encrypted, iv, authTag] = auth.encryptWithAES(text)
// const encrypted = auth.encryptWithAES(text)
// const decrypted = auth.decryptWithAES(text2, encrypted);
// console.log(decrypted)
